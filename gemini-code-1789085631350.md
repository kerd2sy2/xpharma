# Agent Execution Instructions
Read docs: https://docs.google.com/document/d/10EQ0YE2SyJsAY933Tr6suTf-GLJJZ5bYGfb1HwwXg1g/edit

Rules:
- Caveman style responses (minimal tokens).
- Strictly sequential execution (Step-by-step).
- Do not jump to next step until current step is completed and confirmed.

---

### Step 1: Database Setup
- Init PostgreSQL 16 schemas: `public` + `tenant_template`[cite: 1].
- Apply central tables (`tenants`, `pharmacies`, `subscriptions`, `tenant_sync_states`)[cite: 1].
- Apply tenant tables (`invoices`, `invoice_items`, `returns`, `cash_receipts`, `ledger_entries`)[cite: 1].

### Step 2: Go Backend Architecture & Shared Core
- Setup monorepo/modules for Go microservices.
- Write Dynamic Tenant Connection Router (`pgxpool` based on JWT tenant_id).
- Setup Redis connection (Streams + Caching).

### Step 3: Auth & Tenant Service (Port 5001)
- Google OAuth endpoint for Mobile and Admin Dashboard[cite: 1].
- JWT token generator/verifier.
- Tenant management & Agent token generator APIs.

### Step 4: Sync & Ingestion Service (Port 5004)
- HTTPS Gzip Batch Ingestion endpoint[cite: 1].
- `Idempotency-Key` validator[cite: 1].
- Incremental Upsert engine + Redis Stream publisher[cite: 1].
- Daily Reconciliation worker[cite: 1].

### Step 5: Pharmacy Query Service (Port 5002)
- Read-only APIs for Expo app:
  - Ledger / Account Statement[cite: 1]
  - Purchases list + details + date filter[cite: 1]
  - Returns & Cash receipts lists[cite: 1]

### Step 6: Billing & Notification Services
- Billing (Port 5003): InstaPay receipt upload, State Machine (`Pending` -> `Approved`)[cite: 1].
- Notification Worker: FCM push sender via Redis Streams + 28th-day subscription checker[cite: 1].
- Agent heartbeat & 24h offline checker.

### Step 7: Warehouse Windows Agent (tabarak-agent & supervisor)
- Read-only Firebird (`ORGA.GDB`) driver integration[cite: 1].
- Embedded local queue (`bbolt`) for offline tolerance[cite: 1].
- Initial sync (last 6 months chunks) + Delta sync (Cursor)[cite: 1].
- Gzip compression + HTTPS pusher[cite: 1].
- Auto-update module with SHA-256 validation & rollback[cite: 1].

### Step 8: Next.js Admin Dashboard (next-shadcn-dashboard-starter)
- Google OAuth login restricted to Super Admin email[cite: 1].
- Tenants screen (create tenant, token generation, health status)[cite: 1].
- Billing review screen (receipt inspection, approve/reject buttons)[cite: 1].
- Monitoring screen (Agent status, sync lag, error logs)[cite: 1].

### Step 9: Expo Mobile App (Arabic RTL)
- Google Auth screen + Warehouse link code input[cite: 1].
- Dashboard home (warehouse cards + balance)[cite: 1].
- Invoices / Returns / Cash screens + Bottom-sheet date filter[cite: 1].
- Ledger screen (no export, direct view)[cite: 1].
- Profile screen (subscription counter, InstaPay upload, WhatsApp support)[cite: 1].